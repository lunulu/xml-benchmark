build:
	@npm install --silent sax
	@echo "✅ JS sax: dependencies are installed"

run:
	node main.js "$(INPUT)"

clean:
	@rm -rf node_modules package.json package-lock.json
	@echo "🧹 JS sax: temporary files are deleted"
