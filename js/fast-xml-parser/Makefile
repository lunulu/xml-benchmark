build:
	@npm install --silent fast-xml-parser
	@echo "✅ JS fast-xml-parser: dependencies are installed"

run:
	node main.js "$(INPUT)"

clean:
	@rm -rf node_modules package.json package-lock.json
	@echo "🧹 JS fast-xml-parser: temporary files are deleted"
