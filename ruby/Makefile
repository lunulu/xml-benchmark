build:
	@gem list ox -i > /dev/null || gem install ox
	@echo "✅ Ruby: зависимости установлены"

run:
	ruby main.rb "$(INPUT)"

clean:
	@echo "🧹 Ruby: очищать нечего"
