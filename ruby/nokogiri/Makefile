build:
	@gem list nokogiri -i > /dev/null || gem install nokogiri
	@echo "✅ Ruby Nokogiri: dependencies are installed"

run:
	ruby main.rb "$(INPUT)"

clean:
	@echo "🧹 Ruby Nokogiri: nothing to clean up"
